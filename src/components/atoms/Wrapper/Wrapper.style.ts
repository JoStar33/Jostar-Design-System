import { styled } from 'styled-components';

export const S = {
  Wrapper: styled.div`
    .wrapper {
      &__label-text {
        font-size: 14px;
        font-weight: 500;
        margin-bottom: 10px;
      }
    }
  `,
};
