import { styled } from 'styled-components';

export const S = {
  FormWrapper: styled.form`
    .form-wrapper {
      &__container {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
    }
  `,
};
